import React from 'react'

export default function page() {
    //check if user has a inventory created

    //If they dont then we will create one

    //Then use that Id to create items in the db

    
  return (
    <div>

    </div>
  )
}
